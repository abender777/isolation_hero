part of time_counter_widget;

class _TimeCounterTablet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('time_counter_tablet'),
    );
  }
}